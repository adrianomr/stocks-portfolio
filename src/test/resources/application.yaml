spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/stocks-portfolio
    username: postgres
    password: postgres
  jpa.hibernate.ddl-auto: validate

amazon:
  accessKey: test
  secretKey: test

gateway:
  url:
    stocks: http://localhost:8089/stocks

server:
  tomcat:
    max-threads: 200
